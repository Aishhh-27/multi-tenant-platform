apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab-app.fullname" . }}-test-connection"
  labels:
    {{- include "gitlab-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitlab-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
